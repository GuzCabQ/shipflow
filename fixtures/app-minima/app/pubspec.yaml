name: app
description: "Capa de UI del fixture. Depende del dominio por ruta."
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.6.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dominio:
    path: ../dominio

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
