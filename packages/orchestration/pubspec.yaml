name: orchestration
description: "Compositor de etapas, AutonomyPolicy, cascada, presupuestos."
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  core:
    path: ../core
