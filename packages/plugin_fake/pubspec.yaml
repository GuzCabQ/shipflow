name: plugin_fake
description: "Implementacion completa de todos los puertos, para pruebas."
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  core:
    path: ../core
