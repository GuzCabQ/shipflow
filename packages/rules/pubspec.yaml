name: rules
description: "Registro de reglas, proyeccion a C y E, telemetria por regla."
publish_to: none
version: 0.1.0

environment:
  # Alineado con lo que el lockfile REQUIERE de verdad: declaraba `^3.6.0` y el
  # lock ya exigía >=3.11, así que era un piso que nadie podía cumplir. El
  # fixture se corrigió por esto mismo y no se propagó acá.
  sdk: ^3.11.0

resolution: workspace

# Sin dependencias: este paquete todavía no tiene API —«la fase 0 instala los
# controles, no el producto»— y no importa nada. `arquitectura.json` le permite
# ver a `core`; la flecha vuelve en el mismo commit que la use.
