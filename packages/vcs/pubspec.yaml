name: vcs
description: "Rama y commits LOCALES. No conoce la forja ni empuja nada. Agnostico del lenguaje y del agente."
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  core:
    path: ../core

# `vcs` no es `core`: puede declarar sus dependencias de desarrollo.
dev_dependencies:
  test: ^1.25.0
